import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Badge from '@knack-ux/badge';
import { version } from '@knack-ux/badge/package.json';

<Meta title="Components|Badge" />

# Badge
<span>Latest Version: {version}</span>

The text color of the badge component will be calculated
based on the background color that you specify. It can be
either black or white, depending on which of the two is
more a11y contrast friendly!

```
  import Badge from '@knack-ux/badge';
```

<Props of={Badge} />

## Variants
<Preview>
  <Story name="Variants">
    <>
      <Badge mr="8px">Info (default)</Badge>
      <Badge variant="success" mr="8px">Success</Badge>
      <Badge variant="warning" mr="8px">Warning</Badge>
      <Badge variant="danger" mr="8px">Danger</Badge>
      <Badge variant="notspecified" mr="8px">Not Specified</Badge>
      <Badge bg="purple" mr="8px">Custom Purple</Badge>
      <Badge bg="yellow" >Custom Yellow</Badge>
    </>
  </Story>
</Preview>