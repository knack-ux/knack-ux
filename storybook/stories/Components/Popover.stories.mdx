import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Popover, { Trigger, Content } from '@knack-ux/popover';
import Menu, { Item, Divider } from '@knack-ux/menu';
import Avatar from '@knack-ux/avatar';
import Button from '@knack-ux/button';
import { version } from '@knack-ux/popover/package.json';

<Meta title="Components|Popover" />

# Popover
<span>Latest Version: {version}</span>

```ts
  import Popover from '@knack-ux/popover';
```

<Props of={Popover} />

## Basic
<Preview>
  <Story name="Basic" height="240px">
    <Popover>
      <Trigger>
        <Button>
          Options
        </Button>
      </Trigger>
      <Content width="160px" p="8px">
        <Menu id="basic-menu" title="Options">
          <Item icon="share">Share</Item>
          <Item icon="shuffle">Move</Item>
          <Item icon="edit">Rename</Item>
          <Divider />
          <Item intent="danger" icon="delete_outline">Delete</Item>
        </Menu>
      </Content>
    </Popover>
  </Story>
</Preview>


## Avatar
<Preview>
  <Story name="Avatar" height="240px">
    <Popover>
        <Trigger>
            <Avatar name="Kristiyan Serafimov" />
        </Trigger>
        <Content width="160px" p="8px" style={{ position: 'absolute' }}>
          <Menu id="profile-menu">
            <Item icon="share">Share</Item>
          </Menu>
        </Content>
      </Popover>
  </Story>
</Preview>